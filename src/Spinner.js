import React from "react"; 

const Spinner = () => {
    return (
        <div class="ui active dimmer">
        <div class="ui big text loader">Loading, Please wait...</div>
      </div>
        );
    };

export default Spinner;